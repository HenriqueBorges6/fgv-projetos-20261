# Segredos e config gerada — nunca commitar
scripts/.env
scripts/pipeline_info.json
.env
pipeline_info.json
__pycache__/

# Terraform (infra/)
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.backup
infra/*.tfstate.lock.info
infra/terraform.tfvars
infra/lambda/*.zip

# Dump completo do banco — não commitar (reusa o já versionado no A1)
data/*.sql

# Evidências geradas (mantém só o README guia)
evidence/*.txt
evidence/*.png
